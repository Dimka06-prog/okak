# Предать или Сотрудничать

Многопользовательская игра на основе дилеммы заключенного с реальным временем.

## 🎨 Версия 2.0.0 - Полный рефакторинг

Приложение полностью переработано с современным дизайном и улучшенной архитектурой!

### ✨ Что нового в v2.0.0

- 🎨 **Современный UI** с четкой видимостью всех элементов
- 📱 **Адаптивный дизайн** под разные размеры экранов
- 🎭 **Анимации** и плавные переходы
- 🎯 **Улучшенная UX** с горячими клавишами и валидацией в реальном времени
- 🏗️ **Чистая архитектура** с разделением на слои
- 🔧 **Рефакторинг кода** с устранением дублирования

## Архитектура проекта

Проект построен по принципу чистой архитектуры с разделением на слои:

```
src/
├── config/                 # Конфигурация приложения
│   ├── config_manager.py   # Менеджер конфигурации
│   ├── app_config.json     # Конфигурация приложения
│   └── firebase_config.json # Конфигурация Firebase (не в Git)
├── data/                   # Слой данных
│   ├── models/            # Модели данных
│   ├── repositories/      # Репозитории
│   └── database/          # Реализации баз данных
├── business/              # Бизнес-логика
│   ├── services/          # Сервисы
│   ├── game_logic/        # Логика игры
│   └── exceptions.py      # Исключения
├── presentation/          # Презентационный слой
│   ├── views/             # Представления (UI)
│   │   ├── base_view.py   # Базовый класс представлений
│   │   └── login_view.py  # Экран входа
│   ├── controllers/       # Контроллеры
│   ├── styles.py          # Менеджер стилей
│   └── viewmodels/        # ViewModels
└── utils/                 # Утилиты
    └── ui_helpers.py      # Помощники UI
```

## 🎨 Улучшения интерфейса

### Централизованная система стилей
- **StyleManager**: Единый менеджер всех стилей приложения
- **Цветовая схема**: Профессионально подобранные цвета
- **Адаптивность**: Автоматическая подстройка под размер экрана
- **Темная тема**: Автоматическое определение системной темы

### Улучшенная видимость
- **Четкие поля ввода**: Яркие границы и хороший контраст
- **Крупные шрифты**: Улучшенная читаемость
- **Контрастные кнопки**: Понятные состояния (наведение, нажатие)
- **Анимации**: Плавные переходы и микро-взаимодействия

### UX улучшения
- **Горячие клавиши**: Enter для отправки форм
- **Валидация в реальном времени**: Мгновенная обратная связь
- **Состояния загрузки**: Индикаторы процессов
- **Заставка**: Профессиональный экран загрузки

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Настройте Firebase:
   - Скопируйте `src/config/firebase_config.json.example` в `src/config/firebase_config.json`
   - Заполните вашими учетными данными Firebase

3. Запустите приложение:
```bash
python app.py
```

## Особенности архитектуры

### 🏗️ Чистая архитектура
- **Data Layer**: Модели данных, репозитории, реализации баз данных
- **Business Layer**: Сервисы, бизнес-логика, исключения
- **Presentation Layer**: Views, Controllers, ViewModels

### 🔒 Безопасность
- Пароли хешируются с использованием bcrypt
- Учетные данные Firebase хранятся в отдельном конфигурационном файле
- Файл `firebase_config.json` добавлен в `.gitignore`

### 🛠️ Паттерны проектирования
- **Repository Pattern**: Абстракция доступа к данным
- **Service Layer**: Инкапсуляция бизнес-логики
- **MVC**: Разделение Model-View-Controller
- **Dependency Injection**: Внедрение зависимостей
- **Base Classes**: Уменьшение дублирования кода

### 🔄 Надежность
- Автоматическое восстановление соединения
- Обработка исключений на всех уровнях
- Подробное логирование
- Graceful degradation

## 🎨 Система стилей

### Цветовая палитра
- **Primary**: #2196F3 (синий)
- **Success**: #4CAF50 (зеленый)
- **Warning**: #FF9800 (оранжевый)
- **Danger**: #f44336 (красный)
- **Info**: #00BCD4 (голубой)

### Компоненты
- **Кнопки**: 4 варианта (primary, success, warning, danger)
- **Поля ввода**: Состояния (normal, focus, error)
- **Карточки**: Тени и скругления
- **Тексты**: 4 варианта (primary, secondary, muted, status)

### Адтивность
- Автоматическое центрирование окна
- Минимальные и максимальные размеры
- Прокрутка для маленьких экранов
- Отзывчивые отступы

## Конфигурация

### Приложение (`src/config/app_config.json`)
```json
{
    "app": {
        "name": "Предать или Сотрудничать",
        "version": "2.0.0",
        "debug": false
    },
    "ui": {
        "window_width": 800,
        "window_height": 600,
        "theme": "default"
    }
}
```

### Firebase (`src/config/firebase_config.json`)
Заполните вашими данными из консоли Firebase.

## Правила валидации

- **Username**: 3-20 символов, буквы, цифры, _ и -
- **Password**: минимум 6 символов
- **Валидация в реальном времени**: мгновенная обратная связь

## 🚀 Горячие клавиши

- **Enter**: Переключение между полями и отправка форм
- **Tab**: Стандартная навигация между полями
- **Escape**: Закрытие диалоговых окон

## Логирование

Логи выводятся в консоль с форматом:
```
%(asctime)s - %(name)s - %(levelname)s - %(message)s
```

Уровень логирования настраивается в `app_config.json`.

## Сборка в .exe

Для создания исполняемого файла:
```bash
pyinstaller --onefile --windowed app.py
```

## 🧪 Тестирование

Структура готова для добавления тестов:
```bash
# Создать папку для тестов
mkdir -p tests/{unit,integration,ui}

# Запустить тесты (когда будут добавлены)
python -m pytest tests/
```

## 📱 Скриншоты

### Экран входа
- Современный дизайн с карточками
- Четкие поля ввода с валидацией
- Анимированные кнопки
- Адаптивная верстка

### Главное меню
- Профессиональный интерфейс
- Интуитивная навигация
- Статусные индикаторы

## 🔧 Разработка

### Добавление нового функционала:

1. **Модель данных** → `src/data/models/`
2. **Репозиторий** → `src/data/repositories/`
3. **Бизнес-логика** → `src/business/services/`
4. **Представление** → `src/presentation/views/` (наследовать от BaseView)
5. **Контроллер** → `src/presentation/controllers/`

### Пример добавления нового представления:

```python
from src.presentation.views.base_view import FormView

class NewView(FormView):
    def __init__(self):
        super().__init__()
        self.init_ui()
        self.add_form_field('username', self.username_field, 'username')
        self.add_form_field('password', self.password_field, 'password')
    
    def init_ui(self):
        # Ваш код инициализации UI
        pass
```

## 🐛 Известные проблемы

- Нет темной темы (в планах)
- Отсутствует локализация (в планах)
- Нет иконок приложения (в планах)

## 🗺️ Дорожная карта

- [ ] Темная тема интерфейса
- [ ] Локализация (EN/RU)
- [ ] Иконки приложения
- [ ] Звуковые эффекты
- [ ] Анимации переходов между экранами
- [ ] Настройки пользователя
- [ ] История игр
- [ ] Статистика и достижения

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit ваши изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT - см. файл LICENSE для деталей.
